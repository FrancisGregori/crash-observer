# Crash Game Observer - Configurações de Ambiente

# ============================================
# CONFIGURAÇÕES DO BROWSER (PLAYWRIGHT)
# ============================================

# Modo isolado: cria um diretório de sessão único a cada execução
# Isso evita conflitos com outros processos Playwright
# Desvantagem: você precisará fazer login a cada reinício
# Valores: true | false (default: false)
# BROWSER_ISOLATED=true

# Canal do browser: usar uma instalação diferente do Chrome/Chromium
# Útil quando você tem conflitos com outro processo usando o Chromium padrão
# Valores: chrome | chrome-beta | chrome-dev | chrome-canary | msedge | msedge-beta | msedge-dev
# BROWSER_CHANNEL=chrome-beta

# Diretório customizado para dados do browser
# Se especificado, sobrescreve o diretório padrão
# Exemplo: /tmp/crash-observer-browser
# BROWSER_USER_DATA_DIR=/tmp/crash-observer-browser

# Caminho para executável customizado do browser
# Útil para usar uma instalação específica
# BROWSER_EXECUTABLE=/Applications/Google Chrome Beta.app/Contents/MacOS/Google Chrome Beta

# Modo headless (sem interface gráfica)
# Valores: true | false (default: false)
# BROWSER_HEADLESS=true

# Modo não persistente: não salva sessão do browser
# O browser abre sem dados salvos e não persiste cookies/localStorage
# Vantagem: não bloqueia diretórios, não conflita com outros processos
# Desvantagem: você precisará fazer login a cada execução
# Valores: true | false (default: false)
# BROWSER_NON_PERSISTENT=true

# ============================================
# CENÁRIOS DE USO
# ============================================

# CENÁRIO 1: Rodar observer isolado de outros processos Playwright
# Use BROWSER_ISOLATED=true ou BROWSER_CHANNEL=chrome-beta

# CENÁRIO 2: Rodar sem conflitos de arquivos bloqueados
# Use BROWSER_NON_PERSISTENT=true

# CENÁRIO 3: Usar browser diferente do Chromium
# Use BROWSER_CHANNEL=msedge ou BROWSER_EXECUTABLE=/path/to/browser

# CENÁRIO 4: Rodar em servidor (sem GUI)
# Use BROWSER_HEADLESS=true (nota: precisa de login automatizado ou sessão pré-configurada)
